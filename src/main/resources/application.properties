# ========== SUPABASE DATABASE CONFIGURATION ==========
spring.datasource.url=jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.fxyqlrobrlbobmphsacg
spring.datasource.password=80069499aj
server.port=8080

# ========== JPA CONFIGURATION ==========
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# ========== SSL CONFIGURATION ==========
spring.datasource.hikari.ssl-mode=require
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# ========== CONNECTION POOL ==========
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=30000

# ========== CORS CONFIGURATION (FOR ESP32) ==========
# Allow ESP32 to connect from any IP
cors.allowed-origins=*
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# ========== LOGGING ==========
logging.level.org.springframework.web=DEBUG
logging.level.com.yourpackage=DEBUGserver.port=8080
server.address=0.0.0.0
